    // Success: Native .deb KeePassXC detects and uses YubiKey for database unlock. Snap/AppImage restrictions confirmed.
  // KeePassXC hardware key troubleshooting (April 2026):
  // - Snap package restricts USB access, preventing YubiKey detection.
  // - Solution: Use native .deb or AppImage for full hardware key support.
  // - Commands: sudo snap remove keepassxc && sudo apt install keepassxc
{
  "description": "Ubuntu-specific environment variables and settings for Operations workspace.",
  "example_var": "ubuntu_value",
  "log_path": "./logs/ubuntu/",
  "obsidian_vaults": {
    "work": "/home/tomkw/Obsidian/work-v2-vault",
    "personal": "/home/tomkw/Obsidian/LifeOS-vault"
  }
}